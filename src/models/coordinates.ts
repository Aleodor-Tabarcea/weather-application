export class Coordinates {

  longitude: Number;
  latitude: Number;

  constructor(longitude: Number,
              latitude: Number){
                this.latitude = latitude;
                this.longitude = longitude;
              }
}